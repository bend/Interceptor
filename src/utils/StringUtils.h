#ifndef STRING_UTILS_H__
#define STRING_UTILS_H__

#include <iostream>

namespace StringUtils {
  
  bool containsString(const unsigned char* data, const std::string& pattern, size_t s);

};

#endif // STRING_UTILS_H__
